import { TextconvertPipe } from './textconvert.pipe';

describe('TextconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new TextconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
